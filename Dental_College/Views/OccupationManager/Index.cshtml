@model IEnumerable<Dental_College.Models.PatientOccupation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Occupation_Desc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Occupation_EntryDateTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Occupation_Desc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Occupation_EntryDateTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Occupation_Code">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Occupation_Code">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Occupation_Code">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
