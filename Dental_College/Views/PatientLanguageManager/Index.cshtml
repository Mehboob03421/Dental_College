@model IEnumerable<Dental_College.Models.PatientLanguage>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Language_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Language_EntryDateTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Language_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Language_EntryDateTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Language_Code">Edit</a> |
               @* <a asp-action="Details" asp-route-id="@item.Language_Code">Details</a> |*@
                <a asp-action="Delete" asp-route-id="@item.Language_Code">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
